import React from 'react';

import AppNavigator from './AppNavigator';

const RootNavigator = () => {
  // if (isLoading) {
  //   return <SplashScreen />;
  // } else {
  return <AppNavigator />;
  // }
};

export default RootNavigator;
